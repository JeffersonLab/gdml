ifdef PLATFORM_SUPPORTS_SHARED_LIBS 

  ifeq ($(BUILD_SHARED_LIBS), yes)
    DO_SHARED_LIBS := 1    
  endif

  ifeq ($(BUILD_STATIC_LIBS), yes)
    DO_STATIC_LIBS := 1
  endif

  ifdef DO_STATIC_LIBS
    ifdef DO_SHARED_LIBS
      DO_BOTH_LIBS := 1
    endif
  endif

endif